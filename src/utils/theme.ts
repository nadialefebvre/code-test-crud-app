import { createTheme } from "@mui/material/styles"

export const theme = createTheme({
  palette: {
    primary: {
      main: "#385f73",
      contrastText: "#fff",
    },

    secondary: {
      main: "#d1434c",
      contrastText: "#fff",
    },
  },
})
